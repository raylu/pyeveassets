{% extends 'base.jinja2' %}

{% block body %}
	<a href="/assets">back</a>
	<h2>{{ char_name }}'s assets</h2>

	{% for station, items in all_items %}
		<h3>{{ station }}</h3>
		<div>
			{% for item, quantity, price in items %}
				{% if quantity != 1 %}
					{{ quantity }} ✕
				{% endif %}
				{{ item }}
				{{ (price / 100000000)|round(1) }}
				<br>
			{% endfor %}
		</div>
	{% endfor %}
{% endblock body %}
